# Travis CI configuration file for running tests
language: python
branches:
  only:
    - laq/travisTest
services:
  - mongodb

notifications:
  email: false
python:
  - "2.7"
install:
  #- "sudo apt-get install -y build-essential software-properties-common python-software-properties curl git-core libxml2-dev libxslt1-dev libfreetype6-dev python-pip python-apt python-dev tree"
  - "sudo pip install --upgrade pip"
  - "sudo pip install --upgrade virtualenv"
  - "pip install requirements/edx/paver.txt"
script:
  - paver test

